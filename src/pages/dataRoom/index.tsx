import React from 'react';

const DataRoom = function () {
  return <iframe src='/dataroom' style={{ width: '100%', height: '100%', overflow: 'hidden' }} scrolling='no'></iframe>;
};

export default DataRoom;
